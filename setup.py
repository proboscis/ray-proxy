from setuptools import setup

setup(
    name="ray-proxy",
    version="0.0.1",
    install_requires=[],
    extras_require={
        "develop": []
    },
    entry_points={
        "console_scripts": [
        ],
        "gui_scripts": [
        ]
    }
)
